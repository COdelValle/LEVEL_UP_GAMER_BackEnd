# 🏗️ Arquitectura de Seguridad - LEVEL UP GAMER

## Flujo de Autenticación

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLIENTE (Frontend)                          │
└──────────────────────────────┬──────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
              [REGISTRO]            [LOGIN]
                    │                     │
        ┌───────────▼──────────┐  ┌──────▼──────────┐
        │ POST /api/auth/register│  │ POST /api/auth/login
        │ {nombre, email,       │  │ {email, password}
        │  password}            │  │
        └───────────┬───────────┘  └──────┬──────────┘
                    │                     │
                    └──────────┬──────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │        AuthController                      │
        │  ┌────────────────────────────────────┐   │
        │  │ 1. Validar @Valid DTO              │   │
        │  │ 2. Verificar duplicados (registro) │   │
        │  │ 3. Encriptar contraseña            │   │
        │  │ 4. Generar JWT Token               │   │
        │  │ 5. Generar/Recuperar API Key       │   │
        │  │ 6. Guardar en BD                   │   │
        │  └────────────────────────────────────┘   │
        └──────────────────────┬─────────────────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │        Servicios                           │
        │  ┌────────────────────────────────────┐   │
        │  │ • UsuarioService                   │   │
        │  │ • PasswordEncrypter                │   │
        │  │ • JwtProvider                      │   │
        │  └────────────────────────────────────┘   │
        └──────────────────────┬─────────────────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │        Base de Datos (Oracle)              │
        │  ┌────────────────────────────────────┐   │
        │  │ USUARIO                            │   │
        │  │ ├─ id: Long (PK)                   │   │
        │  │ ├─ nombre: String                  │   │
        │  │ ├─ email: String (UNIQUE)          │   │
        │  │ ├─ password: String (BCRYPT)       │   │
        │  │ ├─ apiKey: String (UNIQUE)         │   │
        │  │ ├─ rol: RolUsuario                 │   │
        │  │ ├─ fechaCreacion: LocalDateTime    │   │
        │  │ └─ ultimoAcceso: LocalDateTime     │   │
        │  └────────────────────────────────────┘   │
        └──────────────────────┬─────────────────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │        Response (AuthResponse)             │
        │  {                                         │
        │    "token": "eyJhbGc...",                 │
        │    "apiKey": "base64...",                 │
        │    "usuarioId": 1,                         │
        │    "nombre": "Juan Pérez",                │
        │    "email": "juan@example.com",           │
        │    "rol": "USER"                          │
        │  }                                        │
        └──────────────────────┬─────────────────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │     CLIENTE (Almacena Token/API Key)      │
        └──────────────────────┬─────────────────────┘
                               │
```

## Flujo de Solicitudes Protegidas

```
┌──────────────────────────────────────────────────────────┐
│              CLIENTE (Frontend)                          │
│  GET /api/usuarios                                      │
│  Authorization: Bearer <JWT_TOKEN>                      │
│      OR                                                  │
│  X-API-Key: <API_KEY>                                   │
└──────────────────────────┬───────────────────────────────┘
                           │
        ┌──────────────────▼────────────────────┐
        │   JwtAuthenticationFilter             │
        │  ┌──────────────────────────────────┐ │
        │  │ 1. Extrae token de header        │ │
        │  │ 2. Valida firma JWT              │ │
        │  │ 3. Valida expiración             │ │
        │  │ 4. Extrae usuarioId y email      │ │
        │  │ 5. Crea Authentication token     │ │
        │  │ 6. Establece contexto seguridad  │ │
        │  └──────────────────────────────────┘ │
        └──────────────────┬────────────────────┘
                           │
                    [¿Válido?]
                      /    \
                   SI/      \NO
                   /          \
        ┌─────────▼──┐    ┌────▼────────┐
        │ Continúa   │    │ 401 Unauth. │
        │ Request    │    └─────────────┘
        └─────────┬──┘
                  │
        ┌─────────▼──────────────────────┐
        │   UsuarioController            │
        │  ┌───────────────────────────┐ │
        │  │ getUsuarios()             │ │
        │  │ @Secured("USER")          │ │
        │  └───────────────────────────┘ │
        └─────────┬──────────────────────┘
                  │
        ┌─────────▼──────────────────────┐
        │   UsuarioService               │
        │  ┌───────────────────────────┐ │
        │  │ List<Usuario> getUsuarios │ │
        │  └───────────────────────────┘ │
        └─────────┬──────────────────────┘
                  │
        ┌─────────▼──────────────────────┐
        │   Base de Datos                │
        │  SELECT * FROM usuario         │
        └─────────┬──────────────────────┘
                  │
        ┌─────────▼──────────────────────┐
        │   Response (200 OK)            │
        │  [List<Usuario>]               │
        └────────────────────────────────┘
```

## Stack de Tecnologías

```
┌─────────────────────────────────────────────────────┐
│                   Spring Boot 3.5.7                 │
│                   (Java 21)                         │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  Spring Security (3.2.x)                   │   │
│  │  ├─ AuthenticationManager                  │   │
│  │  ├─ SecurityFilterChain                    │   │
│  │  └─ JwtAuthenticationFilter                │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  JJWT (0.12.3)                             │   │
│  │  ├─ Token Generation                       │   │
│  │  ├─ Token Validation                       │   │
│  │  └─ Claims Extraction                      │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  BCrypt (Spring Security Crypto)           │   │
│  │  ├─ Password Encoding                      │   │
│  │  ├─ Password Matching                      │   │
│  │  └─ API Key Generation                     │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  Jakarta Bean Validation                   │   │
│  │  ├─ @NotBlank, @NotNull                    │   │
│  │  ├─ @Email, @Size, @Min/@Max               │   │
│  │  └─ @Pattern (Regex)                       │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  Spring Data JPA                           │   │
│  │  ├─ User Repository                        │   │
│  │  └─ Oracle Database Integration            │   │
│  └────────────────────────────────────────────┘   │
│                                                     │
│  ┌────────────────────────────────────────────┐   │
│  │  Lombok (Generación de código)             │   │
│  │  ├─ @Data, @AllArgsConstructor             │   │
│  │  └─ @NoArgsConstructor                     │   │
│  └────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

## Matriz de Seguridad

| Aspecto | Implementado | Técnica | Estado |
|---------|-------------|---------|--------|
| **Validación de Entrada** | ✅ | Jakarta Bean Validation | Activo |
| **Almacenamiento Contraseña** | ✅ | BCrypt (10+ rondas) | Seguro |
| **Generación API Key** | ✅ | SecureRandom + Base64 | Seguro |
| **Tokens JWT** | ✅ | HS512 (256-bit key) | Activo |
| **Expiración Tokens** | ✅ | 24 horas | Configurable |
| **Autenticación** | ✅ | JWT + API Key | Dual |
| **Autorización** | ✅ | Spring Security Filter | Activo |
| **Manejo Excepciones** | ✅ | GlobalExceptionHandler | Implementado |
| **CSRF Protection** | ✅ | Deshabilitado (REST) | Adecuado |
| **CORS** | ✅ | Permitidos todos orígenes | Configurable |
| **Session Management** | ✅ | STATELESS | Implementado |
| **Auditoría Acceso** | ⏳ | Timestamp campos | Parcial |

## Archivos Creados

```
BackEnd/
├── src/main/java/com/level_up_gamer/BackEnd/
│   ├── Controller/
│   │   ├── AuthController.java          ✨ NUEVO
│   │   └── UsuarioController.java       ✏️ ACTUALIZADO
│   │
│   ├── DTO/
│   │   ├── LoginRequest.java            ✨ NUEVO
│   │   ├── RegisterRequest.java         ✨ NUEVO
│   │   ├── AuthResponse.java            ✏️ ACTUALIZADO
│   │   ├── CreateProductoRequest.java   ✨ NUEVO
│   │   ├── ProductoResponse.java        ✨ NUEVO
│   │   ├── CreateOrdenRequest.java      ✏️ ACTUALIZADO
│   │   ├── OrdenItemRequest.java        ✨ NUEVO
│   │   ├── CreateBlogRequest.java       ✏️ ACTUALIZADO
│   │   └── UpdateUsuarioRequest.java    ✏️ ACTUALIZADO
│   │
│   ├── Security/
│   │   ├── JwtProvider.java             ✨ NUEVO
│   │   ├── PasswordEncrypter.java       ✨ NUEVO
│   │   └── JwtAuthenticationFilter.java ✨ NUEVO
│   │
│   ├── Config/
│   │   └── SecurityConfig.java          ✨ NUEVO
│   │
│   ├── Exception/
│   │   ├── GlobalExceptionHandler.java  ✏️ ACTUALIZADO
│   │   ├── ResourceNotFoundException.java✨ NUEVO
│   │   └── ConflictException.java       ✨ NUEVO
│   │
│   └── Model/Usuario/
│       └── Usuario.java                 ✏️ ACTUALIZADO (con validaciones)
│
├── src/test/java/
│   └── AuthControllerTests.java         ✨ NUEVO
│
├── src/main/resources/
│   └── application.properties           ✏️ ACTUALIZADO
│
└── pom.xml                              ✏️ ACTUALIZADO (dependencias JWT)
```

## Próximas Fases Recomendadas

### Fase 2: Gestión Avanzada
- [ ] Implementar Refresh Tokens
- [ ] Agregar Rate Limiting
- [ ] Configurar CORS específico
- [ ] Logging y Auditoría

### Fase 3: Autorización
- [ ] Roles y Permisos Granulares
- [ ] @PreAuthorize en endpoints
- [ ] Control de acceso por recurso

### Fase 3: Operaciones
- [ ] Health Checks
- [ ] Métricas (Micrometer)
- [ ] Distributed Tracing
